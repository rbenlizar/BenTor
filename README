######################################################################################################################################################################
#####################################################################################################################################################################

BenTor is a basic tool for leveraging the tor network to issue web commands through Linux CLI.

Dependencies:

	BenTor requires that torsocks and tor installed

To Run:

	Copy the script into your desired directory. Use the command "bash" followed by the script file

Usage:

	bash BenTor.sh -t (testing that tor has properly assigned a different ip address for your web traffic)

Options:

	-c (uses cUrl)
	-w (uses wget)
	-t (tests ip assignment)
	-h (help menu)


Please provide feedback, and keep an eye out for version 2 :-) 
